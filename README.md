# Vector Search

This repository contains the same vector-search CLI implemented in Python (`python-cli/`) and Node.js (`nodejs-cli/`).

## What's common
- JSON input
- Embedding + similarity search flow
- Optional reuse of precomputed vectors

The Node.js version is intended to provide an experimentation environment for JavaScript and web browsers.
